{#
 * Created by PhpStorm.
 * User: Nemzag aka Gaz‑mên Arifi (Shkypi, 1979-09-30) {https://github.com/Nemzag/}.
 * Date: 2020-01-15
 * Time: 12h12
 * Updated:
#}
{% extends 'base.html.twig' %}

{% block title %}Éditer profil utilisateur{% endblock %}

{% block main %}

    <div class = "courses-area" style = "margin-bottom: 30px;" >

        <div class = "container" >

            <div class = "row align-items-center" >
                <div class = "col-lg-12 col-md-12 col-sm-12 col-xs-12" >

                    <div class = "product-payment-inner-st" >

                        <br >

                        <h1 >Éditer profil utilisateur</h1 >

                        <div class = "alert alert-info" >Cette méthode est opérationnelle,<br>
                            bien que j’aie galéré avec une erreur de type :<br>
                            <code >"Serialization of 'Symfony\Component\HttpFoundation\File\File' is not allowed"</code >,<br > que j'ai mis des jours et solutionné grâce à ces pages :
                            <ul >
                                <li >
                                    <a href = "https://www.grafikart.fr/forum/topics/30746" target = "_blank" >https://www.grafikart.fr/forum/topics/30746</a >
                                </li >
                                <li >
                                    <a href = "https://symfony.com/doc/2.4/cookbook/security/entity_provider.html#the-data-model" target = "_blank" >https://symfony.com/doc/2.4/cookbook/security/entity_provider.html#the-data-model</a >
                                </li >
                            </ul >
                        </div >


                        {% for message in app.flashes('public_user_danger') %}
                            <div class = "alert alert-danger" >
                                {{ message }}
                            </div >
                        {% endfor %}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {#
                        {{ form_start(userForm) }}
                        {{ form_widget(userForm) }}
                        <button class="btn">{{ button_label|default('Save') }}</button>
                        {{ form_end(userForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {# Form_Rest #}
                        {#
                        {{ form_start(userForm) }}

                        {{ form_rest(userForm) }}

                        <button class = "btn btn-success gaz-admin-form-button" >{{ button_label|default('Sauver') }}</button >

                        {{ form_end(userForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {{ form_start(userForm) }}

                        {{ include('public/user/_form.html.twig', {'button_label': 'Sauver'}) }}

                        {{ form_end(userForm) }}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {#
                        {{ form_start(userForm) }}
                        <div class = "form-group" >

                            {{ form_label(userForm.role) }}
                            {{ form_widget(userForm.role) }}

                        </div >

                        <button class = "btn btn-success gaz-admin-form-button" >{{ button_label|default('Sauver') }}</button >

                        {{ form_end(userForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        <hr >

                        {% if errors|length > 0 %}
                            {{ dump(errors) }}
                        {% endif %}


                    </div >
                    <!-- product-payment-inner-st" -->
                </div >
                <!-- col-lg-12 col-md-12 col-sm-12 col-xs-12 -->

            </div >
            <!-- row align-items-center -->

        </div >
        <!-- container -->

    </div >
    <!-- courses-area -->

{% endblock %}
