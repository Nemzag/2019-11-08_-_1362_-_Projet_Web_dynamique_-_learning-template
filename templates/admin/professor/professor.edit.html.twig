{% extends 'base_admin.html.twig' %}

{% block title %}Éditer un professeur{% endblock %}

{% block main %}

    <div class = "courses-area" style = "margin-bottom: 30px;" >

        <div class = "container" >

            <div class = "row align-items-center" >
                <div class = "col-lg-12 col-md-12 col-sm-12 col-xs-12" >

                    <div class = "product-payment-inner-st" >

                        <h1 >Éditer un professeur</h1 >

                        {% for message in app.flashes('admin_user_danger') %}
                            <div class = "alert alert-danger" >
                                {{ message }}
                            </div >
                        {% endfor %}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {{ form_start(professorForm) }}

                        {{ include('admin/professor/_form.edit.html.twig', {'button_label': 'Sauver'}) }}

                        {{ form_end(professorForm) }}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {#
                        {{ form_start(userForm) }}
                        <div class = "form-group" >

                            {{ form_label(userForm.role) }}
                            {{ form_widget(userForm.role) }}

                        </div >

                        <button class = "btn btn-success gaz-admin-form-button" >{{ button_label|default('Sauver') }}</button >

                        {{ form_end(userForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        <hr >

                        {% if errors|length > 0 %}
                            {{ dump(errors) }}
                        {% endif %}

                        <button class = "btn btn-primary gaz-admin-form-button" >
                            <a href = "{{ path('admin_user_index') }}" style = "color: white;" >Retour à la liste</a >
                        </button >

                    </div >
                    <!-- product-payment-inner-st" -->
                </div >
                <!-- col-lg-12 col-md-12 col-sm-12 col-xs-12 -->

            </div >
            <!-- row align-items-center -->

        </div >
        <!-- container -->

    </div >
    <!-- courses-area -->

{% endblock %}
