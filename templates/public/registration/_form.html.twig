{{ form_start(formCourseRegistration) }}

<b ><label >Choix du cours</label ></b >

{{ form_widget(formCourseRegistration.course | { "1" } ) }}
{# {{ form_widget(attribute(formCourseRegistration, 'course' ~ 'course.price')) }} #}
    {# definition de attribut par fusion des attributs du Recaptcha #}

    {# J'aimerais ajouté le prix dans le placeholder, je pense que il faille utiliser cette fonction merge.
    {% set attr = attr | merge ({class: (attr.class | default('')) ~ ' ' ~ 'g-recaptcha', 'data-sitekey' : key, 'data-callback': id ~ 'RecaptchaSubmit'}) %}
    #}

{# {{ form_widget(formCourseRegistration) }} #}

<br >

<button class = "btn btn-success col" >{{ button_label | default('Sauvegarder') }}</button >

{{ form_end(formCourseRegistration) }}
