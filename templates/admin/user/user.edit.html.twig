{% extends 'base_admin.html.twig' %}

{% block title %}Promouvoir utilisateur{% endblock %}

{% block main %}

    <div class = "courses-area" style = "margin-bottom: 30px;" >

        <div class = "container" >

            <div class = "row align-items-center" >
                <div class = "col-lg-12 col-md-12 col-sm-12 col-xs-12" >

                    <div class = "product-payment-inner-st" >

                        <h1 >Promouvoir un utilisateur</h1 >

                        {% for message in app.flashes('admin_user_danger') %}
                            <div class = "alert alert-danger" >
                                {{ message }}
                            </div >
                        {% endfor %}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {#
                        {{ form_start(adminUserForm) }}
                        {{ form_widget(adminUserForm) }}
                        <button class="btn">{{ button_label|default('Save') }}</button>
                        {{ form_end(adminUserForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {# Form_Rest #}
                        {#
                        {{ form_start(adminUserForm) }}

                        {{ form_rest(adminUserForm) }}

                        <button class = "btn btn-success gaz-admin-form-button" >{{ button_label|default('Sauver') }}</button >

                        {{ form_end(adminUserForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {{ form_start(adminUserForm) }}

                        {{ include('admin/user/_form.edit.html.twig', {'button_label': 'Sauver'}) }}

                        {{ form_end(adminUserForm) }}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        {#
                        {{ form_start(adminUserForm) }}
                        <div class = "form-group" >

                            {{ form_label(adminUserForm.role) }}
                            {{ form_widget(adminUserForm.role) }}

                        </div >

                        <button class = "btn btn-success gaz-admin-form-button" >{{ button_label|default('Sauver') }}</button >

                        {{ form_end(adminUserForm) }}
                        #}

                        {# ──────────────────────────────────────────────────────────────────────── #}

                        <hr >

                        {% if errors|length > 0 %}
                            {{ dump(errors) }}
                        {% endif %}

                        <button class = "btn btn-primary gaz-admin-form-button" >
                            <a href = "{{ path('admin_user_index') }}" style = "color: white;" >Retour à la liste</a >
                        </button >

                    </div >
                    <!-- product-payment-inner-st" -->
                </div >
                <!-- col-lg-12 col-md-12 col-sm-12 col-xs-12 -->

            </div >
            <!-- row align-items-center -->

        </div >
        <!-- container -->

    </div >
    <!-- courses-area -->

{% endblock %}
